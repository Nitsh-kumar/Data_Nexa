export const SEVERITY_LEVELS = ['critical', 'warning', 'info'];

export const ISSUE_COLORS = {
  critical: 'bg-severity-critical text-critical',
  warning: 'bg-severity-warning text-warning',
  info: 'bg-severity-info text-primary-600',
};

export const ACTION_CENTER_POLLING_INTERVAL = 3000;

